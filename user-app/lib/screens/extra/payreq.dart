import 'package:flutter/material.dart';

class PayReqPage extends StatefulWidget {
  const PayReqPage({super.key});

  @override
  State<PayReqPage> createState() => _PayReqPageState();
}

class _PayReqPageState extends State<PayReqPage> {
  @override
  void initState() {
    super.initState();
  }

  @override
  void dispose() {
    super.dispose();
  }

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: SafeArea(
          child: SingleChildScrollView(
              child: Column(
        children: [],
      ))),
    );
  }
}
